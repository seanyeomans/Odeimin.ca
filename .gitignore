# Secrets — never commit
.env

# Uploaded images — managed on the server, not in the repo
images/

# Runtime logs — server-generated, but keep the .htaccess that blocks
# web access to them
logs/*
!logs/.htaccess
